# Copy to .env (gitignored). Every variable here is optional — the site builds and
# runs with no .env at all.

# Show the lil-gui tuning panel on /, /effects and /loop. Inlined at build time,
# so changing it needs a dev-server restart or a rebuild. Anything but "true" is off.
NEXT_PUBLIC_EFFECTS_GUI=false
